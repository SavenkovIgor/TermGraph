rules:
  - id: cmake.methodSpace
    message: Must be space between method name and bracket
    glob: "CMakeLists.txt"
    pattern:
      - /^\s*[a-z_]+\(/

  - id: cpp.qdebug
    message: Remove QDebug
    glob:
      - "**/**.h"
      - "**/**.cpp"
    pattern:
      - /^#include <QDebug>/
