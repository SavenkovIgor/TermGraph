import:
  - includerules.yml
  - nodebug.yml
  - qmlimport.yml

rules:
  - id: cmake.methodSpace
    message: Must be space between method name and bracket
    glob:
      - "CMakeLists.txt"
      - "**/**/CMakeLists.txt"
    pattern:
      - /^\s*[a-z_]+\(/
